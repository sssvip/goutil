package etcdutil

import (
	"testing"
)

func TestETCDWrapper_Client(t *testing.T) {
	/*key := "nice"
	w := NewETCDWrapper("192.168.2.231:32379")
	go func() {
		fmt.Println("start watch key:", key)
		w.ListenChanged(key, func(value string) {
			fmt.Println("listen:", key, value)
		})
	}()
	time.Sleep(1 * time.Second)
	fmt.Println(w.Get(key))
	w.Put(key, "nice2")
	fmt.Println(w.Get(key))
	w.Close()*/
}
